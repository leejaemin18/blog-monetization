# 자동 글쓰기 프로그램 → 크롬 확장 타당성 검토

강의(1·5강)에 나오는 "결제하면 블로그 글을 자동으로 잘 써주는 프로그램"(알팩 프로 / Loword류)을
**크롬 확장프로그램으로 만들 수 있는지** 조사한 문서입니다.

> 조사일 2026-07. 기술 스택·API·정책은 변동됩니다. 실제 구현 전 재확인하세요.

---

## 결론 먼저

- **초안 생성 어시스턴트로는 충분히 가능하고 합리적입니다.** (권장 ✅)
  키워드/제목을 입력하면 우리 스타일 가이드에 맞는 **초안 + 광고표시 + 링크 자리**를 생성해
  워드프레스/네이버 에디터에 넣어주는 형태. **사람이 검수 후 발행.**
- **"완전 무인 자동 발행"은 권장하지 않습니다.** (⚠️ 위험)
  네이버는 자동 포스팅·매크로를 **약관으로 금지**하고 저품질/제재 위험이 큽니다. 워드프레스는
  기술적으로 가능하나(REST API), AI 대량 자동 발행은 스팸·품질 저하로 이어집니다.
- 실제로 시중 프로그램의 핵심 원리는 **① 상품 API로 이미지·제휴링크 자동 삽입 + ② LLM으로 SEO 초안 생성
  + ③ 자동 포스팅**입니다. 이 중 ①②는 확장으로 구현 가능, ③은 위험을 감수해야 합니다.
  (조사: 크몽/티스토리 자동화 사례 다수가 "AI 글은 검색엔 걸려도 **구매 전환은 약하다**"고 지적)

→ **우리 방향: ①②만 하는 "초안 어시스턴트" 확장 + 사람 발행.** `extension/`에 **실제로 쓸 수 있는 개인용 확장**을 넣었습니다.

> **업데이트(2026-07): 개인용 직접 모드로 완성.** 처음엔 "키는 반드시 프록시 서버에"라는 전제로
> 골격만 만들었지만, **혼자 내 PC에서 쓰는 미배포 확장**에서는 서버가 오히려 진입장벽입니다.
> 그래서 **서버 없이 내 API 키를 로컬(`chrome.storage.local`)에 저장해 바로 호출**하는 직접 모드를
> 기본으로 추가했습니다. 프록시 모드는 "여러 기기/사람·키 숨김"이 필요할 때의 **선택지**로 남겨둡니다.
> 이 절충의 조건은 아래 §2에 정리했습니다.

---

## 1. 시중 프로그램이 하는 일 (원리)

| 단계 | 방식 |
|---|---|
| 상품 선정 | 쿠팡 API/키워드 도구로 고수익 상품·키워드 |
| 이미지·링크 | **쿠팡 파트너스 Open API**(딥링크 생성, HMAC-SHA256 인증)로 상품 이미지·수익링크 자동 |
| 글 생성 | Gemini/GPT 등 LLM으로 SEO 초안 |
| 발행 | 워드프레스 API / 매크로로 자동 포스팅, 예약 |

> 쿠팡은 실제 **Open API + 오픈소스 SDK(JS/TS)**, 심지어 상품정보 추출 **크롬 확장 예제**까지 존재
> → 확장으로 상품 이미지·딥링크를 다루는 것은 **선례가 있음**.

## 2. 크롬 확장으로 가능한가? — 아키텍처

가능합니다. Manifest V3 기준 **두 가지 모드**를 지원합니다.

**A) 개인용 직접 모드 (기본 · 서버 불필요)** — 나 혼자 내 PC에서 쓸 때
```
┌─ 크롬 확장 (MV3) ─────────────────────────────────┐
│  Side Panel (UI) ─ 제목·키워드·니치 입력 + ⚙설정      │
│  ⚙설정: 내 API 키·모델 → chrome.storage.local 저장   │
│  Service Worker ─ 저장된 키로 아래를 직접 호출         │
│  Content Script ─ 워드프레스/네이버 에디터에 삽입      │
└───────────────┬───────────────────────────────────┘
                │  x-api-key + anthropic-dangerous-direct-browser-access
                ▼
        api.anthropic.com  ── Claude Messages API
```

**B) 프록시 모드 (선택 · 여러 기기/사람·키 숨김)**
```
확장(⚙설정에 프록시 URL) ──▶ Backend Proxy(내 서버, 키 보관) ──▶ Claude API
```

**키 보관 절충 — 상황에 따라 다르다.**
- **배포·공유하는 확장이라면 키를 절대 넣지 말 것.** 확장 코드/스토리지는 받는 사람이 열어볼 수 있어
  **Anthropic/쿠팡 키가 유출**되고, 내 크레딧으로 비용이 청구됩니다. → **프록시 모드**를 써야 합니다.
- **혼자 내 PC에서 쓰는 미배포 확장이라면**, 키를 `chrome.storage.local`에 저장하는 **직접 모드가 실용적으로
  안전**합니다(내 기기 밖으로 안 나감). 서버 운영 부담이 없어 실사용에 적합합니다.
- 공통: MV3는 **원격 코드 로딩 금지** → 로직은 확장에 내장. 키가 새면 콘솔에서 **즉시 폐기·재발급**.
- 결론: **개인용은 직접 모드(기본), 배포·다중 사용자는 프록시 모드.** 확장은 둘 다 지원합니다.

## 3. 글 생성 엔진 — Claude API

- 모델: **`claude-opus-5`** (Anthropic 최신·최상위. 한국어 품질 우수)
- 방식: Messages API (`client.messages.create`)
- 프록시에서 **우리 스타일 가이드를 system 프롬프트로** 주입 →
  말투/링크 1~3개/광고표시/이미지 자리/FAQ 구조를 지킨 초안 생성
  ([`../templates/제휴글-스타일가이드.md`](../templates/제휴글-스타일가이드.md))
- 생성 후 확장에서 `compliance.py`와 동일한 규칙을 재검사(선택)

> 참고: 강의의 "저작권 문제 전혀 없음"은 틀림. 벤치마크는 **구조만**, 문장 복제 금지.
> AI 초안은 **사람이 사실·링크·광고표시 최종 검수** 후 발행.

## 4. 제휴링크·이미지 — 쿠팡 파트너스 API (선택)

- 쿠팡 파트너스 **Open API**로 딥링크 생성·상품검색·이미지 URL 취득 가능
- 인증: HMAC-SHA256 서명(키를 프록시에 보관)
- ⚠️ 파트너스 승인·이용약관 준수 필요. 상품 **대표 썸네일**만 사용(상세페이지 이미지 금지)
- 이미지 저작권 규칙 → [`21-이미지-전략-무료저작권.md`](21-이미지-전략-무료저작권.md)

## 5. 발행 단계 — 여기서 갈림길

| 방식 | 가능성 | 위험 |
|---|---|---|
| **초안을 에디터에 삽입** (사람이 검수·발행) | ✅ 권장 | 낮음 |
| 워드프레스 REST API 자동 발행 | 가능 | AI 대량 발행 시 스팸·품질 |
| **네이버 블로그 자동 포스팅/매크로** | ❌ | **약관 위반·저품질·계정 제재** |

> 네이버는 공식 글쓰기 API가 없고 자동화에 강한 반봇 정책. **네이버는 손으로 붙여넣고 발행**하세요.
> 확장의 content script는 "초안을 에디터에 넣어주는" 데까지만.

## 6. 정책·리스크 요약

- 🚫 네이버 자동 포스팅/매크로 = 약관 위반
- 🚫 AI 초안 그대로 대량 발행 = 애드센스/검색 스팸 정책 위반, 저품질
- 🚫 API 키를 확장에 하드코딩 = 유출
- ✅ 사람이 광고표시·링크·사실을 최종 검수(휴먼 인 더 루프)
- ✅ 하루 소량, 품질 우선 → 강의의 "하루 5~10개 자동"보다 안전

## 7. 비용 (대략)

| 항목 | 대략 |
|---|---|
| Claude Opus 5 | 입력 $5 / 출력 $25 per 1M 토큰. 글 1편(초안 ~2000자≈3~4k 토큰 출력) ≈ 수십~백원대 |
| 프록시 서버 | 무료 티어(Cloudflare Workers/Vercel/작은 VPS)로 시작 가능 |
| 쿠팡 API | 무료(파트너스 승인 필요) |

> 비용은 모델·토큰·환율로 변동. 소량 테스트로 실측하세요.

## 8. 만들 것 vs 안 만들 것

**만든다 (`extension/`에 실사용 가능한 개인용 확장으로 완성)**
- 제목·키워드·니치·제휴처·채널 입력 UI + ⚙설정(API 키·모델·프록시 URL)
- **직접 모드**: 내 키로 `api.anthropic.com` 호출 → 스타일 가이드 준수 초안 (서버 불필요)
- **프록시 모드(선택)**: 프록시 URL을 넣으면 그쪽으로 위임
- 광고표시 문구 자동 삽입, 링크 **자리표시자**(사람이 실제 딥링크로 교체)
- 워드프레스/네이버 에디터에 초안 삽입(content script)

**안 만든다 (권장 안 함)**
- 네이버 자동 포스팅/매크로
- **키를 넣은 채로 확장을 배포·공유** (개인용 로컬 저장은 허용, 배포는 프록시 모드로)
- 사람 검수 없는 무인 대량 발행

---

## 프로토타입

`extension/` 폴더에 **실제로 쓸 수 있는 개인용 확장**을 넣었습니다(v0.2.0).
- `extension/manifest.json` — MV3, `api.anthropic.com` 호출 권한
- `extension/sidepanel.html` / `sidepanel.js` — 입력·⚙설정(키·모델)·생성·삽입·복사 UI
- `extension/background.js` — **직접 모드**(내 키로 Anthropic 호출) + 스타일 가이드 system 프롬프트 내장, 프록시 모드 폴백
- `extension/content.js` — 에디터에 초안 삽입
- `extension/proxy/server.js` — (선택) **키를 쥐는 백엔드**(배포·다중 사용자용)
- `extension/README.md` — 설치·실행법(직접 모드 빠른 시작 포함)

> 설치·사용법은 `extension/README.md`의 **"빠른 시작(개인용 직접 모드)"** 참고.
> 자동 발행 기능은 의도적으로 넣지 않았습니다(초안 삽입까지만). 발행은 사람이 검수 후 수동.

## 참고
- 쿠팡 Open API 개발자 포털 https://developers.coupangcorp.com/
- Chrome 확장(MV3) https://developer.chrome.com/docs/extensions
- Anthropic Claude API — 모델·사용법은 이 저장소의 claude-api 스킬/공식 문서
