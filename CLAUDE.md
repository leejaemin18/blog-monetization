# CLAUDE.md — 클로드(Claude) 작업 규칙

이 저장소는 **사람(운영자) + Claude + ChatGPT(Codex)** 3자 협업 프로젝트입니다.
협업 전체 절차는 [`COLLABORATION.md`](COLLABORATION.md), ChatGPT 규칙은 [`AGENTS.md`](AGENTS.md) 참고.

## 내 역할 (Claude)
- 제목·키워드 선정, 글 구조 설계, **본문 초안 작성**
- 컴플라이언스 검수: 광고표시 문구, 금지어, 저작권, 사실 확인 자리 표시
- 저장소 정리(문서·데이터·템플릿)

## 글을 쓸 때 항상 지킬 것
- 스타일 가이드: [`templates/제휴글-스타일가이드.md`](templates/제휴글-스타일가이드.md)
- 광고표시 문구를 **첫 부분 텍스트로** 넣는다(제휴 글 필수).
- 금지어: 최고·무조건·100%·완벽·유일·강력추천·초특가.
- 가격·할인·쿠폰을 **지어내지 않는다**. 확인 필요 값은 `(확인 후 기입)`.
- 초안을 다 쓰면 **반드시** 두 검사를 돌린다(발행 워크플로우 첫 스텝에서도 막는다):
  ```
  python3 tools/compliance.py drafts/<초안>.md
  python3 tools/humanize.py  drafts/<초안>.md --fix
  ```
  AI 티 세 가지는 쓰면서부터 피한다 — **`A가 아니라 B` 글당 1회 / 연결어미 뒤 쉼표 금지 /
  100자 넘는 문장 한둘**. 근거는 `docs/운영-가이드.md` 2.6.
- **발행은 GitHub Actions 워크플로우로 한다.** 세션은 블로그에 직접 접근할 수 없다.
  절차·주의사항은 **[`docs/운영-가이드.md`](docs/운영-가이드.md)** 참고 (먼저 읽을 것).
- 발행 후 **Rank Math 포커스 키워드**는 REST로 저장되지 않아 운영자가 직접 입력한다.

## 협업 절차 (ChatGPT와 비동기 회의)
1. **큰 작업 전 GitHub 이슈**를 만들어 계획·질문·이미지 요구사항을 남긴다.
   (라벨: `collab`, 제목 앞에 `[회의]`)
2. 초안 안에 이미지가 필요한 자리마다 **이미지 브리프 주석**을 넣는다:
   ```
   <!-- CODEX_IMAGE_BRIEF: [썸네일|본문N] 비율=16:9 장면="..." 텍스트여백="우상단" 톤="실사" -->
   ```
   → ChatGPT가 이 주석을 읽고 이미지를 만든다.
3. ChatGPT의 이슈 의견(이미지·구조·저작권)을 원고에 반영한다.
4. **PR 생성** — 본문에 이미지 브리프 목록과 확인 필요 항목을 정리한다.
5. ChatGPT가 이미지 추가 + `data/image_ledger.csv`에 기록 + 검토.
6. **병합은 내가 직접 한다** (2026-08-11 운영자 지시 "앞으로 자동으로 머지해").
   PR을 만들고 머지 승인을 기다리지 않는다. squash 머지.

> ⚠️ 내가 작업해도 ChatGPT가 자동 호출되지 않는다. 운영자가 ChatGPT에게
> "클로드 작업 확인해"라고 알려주거나, GitHub 자동화가 있어야 이어진다.

### 자동 머지에도 PR은 계속 만든다
머지가 자동이어도 PR 본문은 그대로 쓴다. 무엇을·왜 바꿨는지 남는 유일한 기록이고,
Codex 자동 검토도 PR에 붙는다(실제로 `image_ledger.csv` 누락을 그렇게 잡았다).

**단, 이런 건 머지 전에 물어본다:**
- 발행된 글의 본문을 바꾸는 변경 (SEO 패치가 날아갈 수 있음)
- 워드프레스·쿠팡 계정 설정에 영향을 주는 변경
- 발행 전 검사를 우회하거나 약화하는 변경 (`skip_gate`, 임계 완화)
- 되돌리기 어려운 삭제

## 에이전트는 쓰지 않는다 (2026-08-19 개정)

운영자 지시로 `marketer`·`writer` 에이전트를 **전부 지웠다** —
"너무 오래걸린다", "에이전트 전부삭제하고 예전으로 돌아간다".
**브리프·초안·검사·발행을 내가 직접 한다.** 왕복이 사라져서 그만큼 빠르고 토큰도 덜 든다.
`.claude/agents/` 를 다시 만들지 마라.

지운 에이전트에만 있던 규칙은 아래로 흡수했고,
실측 사실은 `docs/운영-가이드.md` 「실측으로 확인된 것」에 있다.

### 새 글 쓰기 전 반드시 읽을 것 (기억에 의존하지 말고 매번 실제로)
1. 이 파일(CLAUDE.md)
2. `templates/제휴글-스타일가이드.md` — 특히 **3.5 F 구조 이력표**와 **3.6 A 상투구 목록**
3. `docs/운영-가이드.md` — GEO, AI 티, 배너 요청 방식, 실측 사실
4. `data/content_ledger.csv` — 최근 발행분과 내부링크로 걸 수 있는 실제 슬러그

### 포커스 키워드는 쓰기 전에 정한다 (2026-08-24 ★)

다 쓰고 나서 키워드를 붙이면 **밀도 0** 이 나온다. 실제로 #345가 그랬다.
초안 첫 줄 주석에 포커스 키워드를 적고, 쓰면서 아래 다섯 자리에 심는다.

| 자리 | 방법 |
|---|---|
| 글 제목 또는 Rank Math **SEO 제목** | SEO 제목은 글 제목과 달라도 된다. 키워드를 **앞쪽**에 둔다 |
| 첫 문단 | 도입 첫 두세 문장 안에 한 번 |
| H2 하나 | 소제목 하나를 키워드가 들어가게 짓는다 |
| 썸네일 alt | `thumb_alt` 앞에 키워드를 붙인다 |
| 검색 스니펫 | `seo_description` 을 키워드로 시작한다 |

**본문 5~6회, 밀도 0.5%대로 맞춘다.** Rank Math가 요구하는 1%는 1000단어 글에서
3어절 구를 열 번 박으라는 뜻이라 문장이 망가진다. 점수는 체크리스트지 순위 요인이 아니다.
**URL 항목은 포기한다** — 슬러그가 전부 영문이라 한글 키워드가 들어갈 수 없고,
한 편만 한글 슬러그로 바꾸면 내부링크 규칙이 깨진다.

### 글 쓸 때 지킬 것
- **구조 회전** — 3.5 F 이력표를 보고 **최근 4편과 도입·전개·마무리가 겹치지 않게** 고른다.
  겹칠 수밖에 없으면 **새 유형을 만들고** 이력표에 `(신규)`로 표기한다.
- **GEO** — 도입부에 굵은 한 줄 답을 던진다. **40자 이내여야** 컴플라이언스가 GEO 요약으로 인정한다.
- **시각자료는 매번 새 형식.** 이미 쓴 것: 좌우 비교표, 소음 눈금자, 구토 감별 카드,
  부위 지도, 경로 단면도, 층 연결도(노드 맵).
- **카드 구성 순서**: ① 글의 결론과 잇는 한 문장 ② 고를 조건 → 이 제품이 맞는 이유
  ③ 쓰는 법 ④ 솔직한 단점 ⑤ 이런 분께 ⑥ CTA 하나
- **카드에 쿠팡 배너 이미지를 넣지 않는다**(`coupangcdn` 0개). **가로 flex 금지**(`display:flex` 0개).
  모바일에서 `160+22+260=442px`라 줄바꿈이 나고 세로 배너가 첫 화면을 먹는다.
  이게 쿠팡 클릭 0의 실측 원인이었다(2026-08-19).
- **CTA는 매번 다르게.** 글을 읽어야만 뜻이 서는 문구로. 같은 문형을 두 편 연속 쓰면
  그 자체가 새 패턴으로 굳는다.
- **딥링크를 초안 본문에 직접 박지 않는다.** 초안에 `link.coupang.com`이 있으면
  `compliance.py`의 `c_disclosure`가 ERROR를 낸다. 인라인 링크도
  `assets/products/<key>-inline.html` 인클루드로 빼고 `[[PRODUCT:<key>-inline]]`을 쓴다.
- **상품 카드 HTML의 주석은 발행본에 그대로 실린다**(초안 주석만 워크플로우가 지운다).
  카드 파일에 `display:flex`·`coupangcdn` 같은 단어를 예시로도 쓰지 마라 — 검사에 걸린다.
- **외부 링크는 걸기 전에 살아 있는지 확인한다.** 세션이 막히면 `fetch-page.yml`로 러너에서 연다.
  확인 못 한 링크를 발행하고 운영자에게 눌러봐 달라고 하지 마라.

### 이미지 — OpenAI API를 쓰지 않는다 (2026-08-20 운영자 지시 ★)

**허락 없이 OpenAI 이미지 API를 절대 호출하지 마라.** ChatGPT Plus가 만든 것을 받아 쓴다. 비용 0.

#### 이미지는 ChatGPT, 글씨는 내가 (2026-08-26 운영자 지시 ★)
운영자: "니가 직접만드는 이미지는 퀄리티가 너무 안좋다", "더 필요한 이미지 있으면 제한 없이
이제부터 이메일에 요청하면 돼", "그럴땐 이미지쪽은 챗지피티가 글씨쪽은 네가해".

- **개수 제한 없음.** 썸네일·본문 2장만이 아니라, 장면 컷이 더 필요하면 3장·4장도 이메일로 요청한다.
  메일 본문에 `[본문이미지1]`, `[본문이미지2]`처럼 여러 프롬프트를 적고 슬러그로 파일이 구분되게 한다.
- **데이터 도해는 SVG를 통째로 만들지 마라.** 내가 만든 도식 실루엣은 투박하다.
  대신 **ChatGPT에 글자 없는 바탕 그림을 요청**하고(프롬프트에 "텍스트·숫자 없음" 명시),
  받은 그림을 SVG의 `<image href="...">` 로 깔고 **그 위에 한글 라벨·숫자·화살표만 내가 `<text>`로 얹는다.**
  발행 러너가 Noto Sans KR 로 렌더하므로 글자는 안 깨지고 바탕은 ChatGPT 퀄리티로 나온다.
  → 그림(ChatGPT) + 글씨(나) 분업. AI가 한글·숫자를 못 넣는 문제와 SVG가 투박한 문제를 동시에 푼다.
- 순수 장면·감성 컷(라벨 불필요)은 그냥 ChatGPT 사진을 쓴다.

1. 초안마다 **영문 슬러그 + 썸네일 프롬프트 + 본문이미지 프롬프트**를 정한다.
2. 초안은 조기 발행되지 않게 **`drafts/_pending/`** 에 두고,
   **`leejaemin18@gmail.com`** (2026-08-21 운영자 확인) 으로 아래 메일을 보낸 뒤
   사용자에게 **"보냈어"(완료형)로 슬러그와 함께** 알린다.
   ```
   제목: [MYBLOG-IMG] {슬러그}
   본문:
     슬러그: {영문 슬러그}
     [썸네일 프롬프트]: (팔레트·장면·한글 문구까지 구체적으로, 브랜드 로고 금지)
     [본문이미지 프롬프트]: (썸네일과 같은 톤, 텍스트 금지·숫자만)
   ```
3. 운영자가 **"업로드 완료"** 라고 하면:
   - Drive에서 `title contains '{슬러그}' and mimeType contains 'image/'` 로 **2장** 검색
   - `download_file_content` 로 받는다. 용량이 커서 파일로 떨어지면 `content`(base64)를
     `python base64.b64decode` 로 PNG 저장
   - **`drafts/img/{슬러그}-thumb.png`**, **`-body.png`** 로 저장 (`git add -f`)
   - 대기 초안을 `drafts/` 로 옮겨 커밋·푸시 → 발행

발행 워크플로우는 `img_slug` 를 받아 `drafts/img/<슬러그>-thumb.png`·`-body.png` 를 쓴다.
파일이 없으면 **멈춘다.** `allow_ai_images=y` 로 명시할 때만 OpenAI를 호출하고,
그건 운영자 허락이 있을 때만이다.

### 발행 (2026-08-19 개정 — 한 번에 끝낸다)
`wp-rewrite-post.yml` **한 번**이면 발행 · 슬러그 · 태그 · 스니펫 · 점검까지 끝난다.
`slug`, `tags`, `seo_description` 을 입력에 같이 넣어라. 비우면 그 단계만 건너뛴다.

예전처럼 `wp-set-slug-tags` → `wp-seo-meta` → `wp-inspect` 를 따로 돌리지 마라.
워크플로우를 네 번 띄우고 그때마다 기다리느라 다 잘 돌아도 10분이 넘었다.
(개별 워크플로우는 이미 발행된 글을 고칠 때만 쓴다.)

FAQ가 있으면 `wp-faq-schema` 는 여전히 따로 돌린다.
**Rank Math 포커스 키워드는 REST로 안 들어가 운영자가 직접 입력한다.**

### 스니펫은 입력에 넣지 말고 초안 주석에서 읽힌다 (2026-08-25 ★)
발행 입력(`seo_description`)에 한글을 넣으면 전송 중 특정 글자가 깨진다 —
실측: `쉬→쉐`, `텨→텔`, `컥→컵`. 오늘 이 때문에 발행을 두 번 취소했고,
취소가 글 생성 이후에 걸려 중복 글이 두 번 생겼다(#374·#385, 둘 다 삭제).
그래서 `wp-rewrite-post.yml`을 고쳤다 — **`seo_description`을 비우면 초안의
`<!-- 확정 스니펫: ... -->` 주석을 디스크에서 읽어 excerpt로 저장한다.** 디스크 파일은 안 깨진다.
**앞으로 발행할 때 `seo_description`은 비우고, 스니펫은 초안 주석에만 적는다.**
꼭 입력으로 넘겨야 하면 깨지는 글자(쉬·텨·컥 등 겹받침/이중모음 조합)를 피해서 쓰고
디스패치 echo로 글자를 눈으로 확인한다. 그리고 **발행을 취소해야 할 땐 5초 안에 잡아도
글 생성이 끝났을 수 있으니, 취소 뒤에는 반드시 `wp-list-posts`로 중복을 확인**한다.
중복이 생겼으면 `wp-delete-post`(완전삭제)로 지우고, 슬러그만 바꿀 땐 `wp-set-slug-tags`에
`slug`만 넣는다(영문이라 안 깨짐).

### 썸네일·본문 alt 도 입력에 넣지 말고 초안 주석에서 읽힌다 (2026-08-29 ★)
스니펫과 같은 이유로 `thumb_alt`·`mid_alt`도 입력으로 넘기면 전송 중 깨진다(`끝→끕`, `세척→세첤`).
`wp-rewrite-post.yml`을 고쳤다 — **입력을 비우면(또는 기본값이면) 초안의
`<!-- 확정 썸네일 alt: ... -->` · `<!-- 확정 본문 alt: ... -->` 주석을 디스크에서 읽어 alt로 넣는다.**
**앞으로 alt 는 초안 주석에만 적고 입력은 비운다.** 인포그래픽 alt(`alt.json`)은 원래 디스크라 안 깨졌다.
`mid_anchor`(본문 이미지 넣을 H2)만 입력으로 넘기는데, 영문이 아니라 한글 소제목이면
초안 주석에 그 소제목을 적어 두고 그대로 복사해 넣어라(오타로 앵커가 안 맞으면 이미지가 글 끝에 붙는다).

**`image_dir` 을 반드시 그 글의 SVG 폴더로 지정한다** (예: `assets/images/chuseok-food`).
기본값은 `assets/images` 루트인데, 러너는 그 폴더 바로 아래 SVG만 PNG로 바꾼다.
하위 폴더에 든 인포그래픽은 변환 목록에 없고 `[[IMG:키]]` 가 **빈 문자열로 조용히 치환된다** —
에러도 경고도 안 난다. 2026-08-24 추석 글(#345)이 배치도 없이 발행됐다.
`alt.json` 도 같은 폴더에서 읽으므로 이걸 틀리면 alt 도 같이 날아간다.
발행 후 점검의 「이미지 태그 N개」는 개수가 아니라 **존재만** 보므로 여기서 안 걸린다.
초안에 `[[IMG:` 가 몇 개인지 세고 그 수만큼 본문에 그림이 있는지 눈으로 확인해라.

### 워크플로우가 멈추면 재시도만 반복하지 마라
2026-08-19에 apt 가 세 번 연속 걸렸는데 두 번을 그냥 다시 돌렸다. 시간만 버렸다.
**같은 자리에서 두 번 멈추면 그건 결함이다.** 로그를 보고 원인을 고쳐라.

### git — squash 머지 뒤에는 브랜치를 다시 잡는다
PR을 squash 머지하면 커밋 해시가 갈라진다. 이어서 작업하기 전에
`git fetch origin main && git checkout -B <브랜치> origin/main` 으로 맞춰라.
**단, 브랜치에 아직 머지 안 된 커밋이 있으면 그게 날아간다** — 실제로 한 번 잃었다.
`git log --oneline origin/main..HEAD` 로 먼저 확인하고, 남길 게 있으면 따로 챙겨라.

## 배너는 글당 1개 (2026-08-16 개정 ★)

> 운영자: "쿠팡 파트너스 구매건이 한 개도 없어. 세 개씩 넣으니까 너무 광고성처럼 보이고 집중이 안 된다."
> "결국 이 블로그는 수익이 목적이야."

3종 카드로 다섯 편을 돌렸고 전환은 0이었다. **글당 상품 1개**로 간다.

- **상품은 글의 결론이 가리키는 것 하나.** 카드는 부록이 아니라 **본문 논지의 도착점**이다.
- **위치는 글 끝이 아니라** 그 물건이 필요하다고 납득된 문단 **바로 다음**.
- 나머지 항목은 **상품 없이 정보로만** 쓴다. 신뢰가 올라간다.
- **"셋 다 필요는 없어요" 류로 닫지 않는다.** 단점은 적되 **"이런 분께"**로 닫는다.
- 카드 구성 순서: ① 글의 결론과 잇기 ② 고를 조건 → 이 제품이 맞는 이유 ③ 쓰는 법
  ④ 솔직한 단점 ⑤ 이런 분께 ⑥ CTA 하나
- **가로 레이아웃, 이미지 크게.** 세로 배너를 나란히 놓지 않는다.

운영자에게는 **검색 링크 1개**만 요청한다. 상품군은 글의 논지로 이미 정해진다.

자세한 근거와 체크리스트는 [`docs/운영-가이드.md`](docs/운영-가이드.md) 「배너를 요청하는 방식」.

## 글마다 반드시 다르게 (★)
같은 틀을 반복하면 독자도 검색엔진도 양산형으로 본다.
새 글 쓰기 전 **스타일가이드 3.5 F 이력표**를 보고 **최근 4편과 도입·전개·마무리 조합이 겹치지 않게** 고른다.
굳어버린 상투구 목록(3.6 A)도 확인한다 — `정리하면`, `저도 ~ 기억이 나요`, `관련해서 아래 글도`, `우리 아이` 남발 등.

## 지식iN 홍보
스타일가이드 **8절**을 따른다. 핵심만:
- 답변 본문에서 특정 제품을 추천하지 않는다
- 쿠팡 링크를 답변에 직접 붙이지 않는다
- **블로그 주소는 매 답변에 넣는다**(2026-08-22 운영자 지시)
- **제휴 공개 문구는 넣지 않는다** — 같은 지시. "이견은 안받는다 무조건빼"
  실질 공개는 도착 페이지가 맡는다(모든 글 맨 위에 쿠팡 문구가 들어간다)
- 답변은 **코드블록으로 전달**한다(서식이 딸려가면 배경색·글자색이 깨진다)
- 질문 주소는 **클릭 가능한 링크로** 준다(복붙용 주소 금지)

## 데이터 대장 (발행할 때마다 갱신)
| 파일 | 언제 |
|---|---|
| `data/content_ledger.csv` | 글 발행 시 한 줄 (제목에 쉼표 있으면 따옴표로 감쌀 것) |
| `data/image_ledger.csv` | 인포그래픽 제작 시 한 줄 |
| `templates/제휴글-스타일가이드.md` 3.5 F | 구조 조합 한 줄 |
| `docs/콘텐츠-로드맵.md` | 발행·보류 결정이 바뀔 때 |
