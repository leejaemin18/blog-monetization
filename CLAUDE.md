# CLAUDE.md — 클로드(Claude) 작업 규칙

이 저장소는 **사람(운영자) + Claude + ChatGPT(Codex)** 3자 협업 프로젝트입니다.
협업 전체 절차는 [`COLLABORATION.md`](COLLABORATION.md), ChatGPT 규칙은 [`AGENTS.md`](AGENTS.md) 참고.

## 내 역할 (Claude)
- 제목·키워드 선정, 글 구조 설계, **본문 초안 작성**
- 컴플라이언스 검수: 광고표시 문구, 금지어, 저작권, 사실 확인 자리 표시
- 저장소 정리(문서·데이터·템플릿)

## 글을 쓸 때 항상 지킬 것
- 스타일 가이드: [`templates/제휴글-스타일가이드.md`](templates/제휴글-스타일가이드.md)
- 광고표시 문구를 **첫 부분 텍스트로** 넣는다(제휴 글 필수).
- 금지어: 최고·무조건·100%·완벽·유일·강력추천·초특가.
- 가격·할인·쿠폰을 **지어내지 않는다**. 확인 필요 값은 `(확인 후 기입)`.
- 초안을 다 쓰면 **반드시** 두 검사를 돌린다(발행 워크플로우 첫 스텝에서도 막는다):
  ```
  python3 tools/compliance.py drafts/<초안>.md
  python3 tools/humanize.py  drafts/<초안>.md --fix
  ```
  AI 티 세 가지는 쓰면서부터 피한다 — **`A가 아니라 B` 글당 1회 / 연결어미 뒤 쉼표 금지 /
  100자 넘는 문장 한둘**. 근거는 `docs/운영-가이드.md` 2.6.
- **발행은 GitHub Actions 워크플로우로 한다.** 세션은 블로그에 직접 접근할 수 없다.
  절차·주의사항은 **[`docs/운영-가이드.md`](docs/운영-가이드.md)** 참고 (먼저 읽을 것).
- 발행 후 **Rank Math 포커스 키워드**는 REST로 저장되지 않아 운영자가 직접 입력한다.

## 협업 절차 (ChatGPT와 비동기 회의)
1. **큰 작업 전 GitHub 이슈**를 만들어 계획·질문·이미지 요구사항을 남긴다.
   (라벨: `collab`, 제목 앞에 `[회의]`)
2. 초안 안에 이미지가 필요한 자리마다 **이미지 브리프 주석**을 넣는다:
   ```
   <!-- CODEX_IMAGE_BRIEF: [썸네일|본문N] 비율=16:9 장면="..." 텍스트여백="우상단" 톤="실사" -->
   ```
   → ChatGPT가 이 주석을 읽고 이미지를 만든다.
3. ChatGPT의 이슈 의견(이미지·구조·저작권)을 원고에 반영한다.
4. **PR 생성** — 본문에 이미지 브리프 목록과 확인 필요 항목을 정리한다.
5. ChatGPT가 이미지 추가 + `data/image_ledger.csv`에 기록 + 검토.
6. **병합은 내가 직접 한다** (2026-08-11 운영자 지시 "앞으로 자동으로 머지해").
   PR을 만들고 머지 승인을 기다리지 않는다. squash 머지.

> ⚠️ 내가 작업해도 ChatGPT가 자동 호출되지 않는다. 운영자가 ChatGPT에게
> "클로드 작업 확인해"라고 알려주거나, GitHub 자동화가 있어야 이어진다.

### 자동 머지에도 PR은 계속 만든다
머지가 자동이어도 PR 본문은 그대로 쓴다. 무엇을·왜 바꿨는지 남는 유일한 기록이고,
Codex 자동 검토도 PR에 붙는다(실제로 `image_ledger.csv` 누락을 그렇게 잡았다).

**단, 이런 건 머지 전에 물어본다:**
- 발행된 글의 본문을 바꾸는 변경 (SEO 패치가 날아갈 수 있음)
- 워드프레스·쿠팡 계정 설정에 영향을 주는 변경
- 발행 전 검사를 우회하거나 약화하는 변경 (`skip_gate`, 임계 완화)
- 되돌리기 어려운 삭제

## 에이전트는 쓰지 않는다 (2026-08-19 개정)

운영자 지시로 `marketer`·`writer` 에이전트를 **전부 지웠다** —
"너무 오래걸린다", "에이전트 전부삭제하고 예전으로 돌아간다".
**브리프·초안·검사·발행을 내가 직접 한다.** 왕복이 사라져서 그만큼 빠르고 토큰도 덜 든다.
`.claude/agents/` 를 다시 만들지 마라.

지운 에이전트에만 있던 규칙은 아래로 흡수했고,
실측 사실은 `docs/운영-가이드.md` 「실측으로 확인된 것」에 있다.

### 새 글 쓰기 전 반드시 읽을 것 (기억에 의존하지 말고 매번 실제로)
1. 이 파일(CLAUDE.md)
2. `templates/제휴글-스타일가이드.md` — 특히 **3.5 F 구조 이력표**와 **3.6 A 상투구 목록**
3. `docs/운영-가이드.md` — GEO, AI 티, 배너 요청 방식, 실측 사실
4. `data/content_ledger.csv` — 최근 발행분과 내부링크로 걸 수 있는 실제 슬러그

### 글 쓸 때 지킬 것
- **구조 회전** — 3.5 F 이력표를 보고 **최근 4편과 도입·전개·마무리가 겹치지 않게** 고른다.
  겹칠 수밖에 없으면 **새 유형을 만들고** 이력표에 `(신규)`로 표기한다.
- **GEO** — 도입부에 굵은 한 줄 답을 던진다. **40자 이내여야** 컴플라이언스가 GEO 요약으로 인정한다.
- **시각자료는 매번 새 형식.** 이미 쓴 것: 좌우 비교표, 소음 눈금자, 구토 감별 카드,
  부위 지도, 경로 단면도, 층 연결도(노드 맵).
- **카드 구성 순서**: ① 글의 결론과 잇는 한 문장 ② 고를 조건 → 이 제품이 맞는 이유
  ③ 쓰는 법 ④ 솔직한 단점 ⑤ 이런 분께 ⑥ CTA 하나
- **카드에 쿠팡 배너 이미지를 넣지 않는다**(`coupangcdn` 0개). **가로 flex 금지**(`display:flex` 0개).
  모바일에서 `160+22+260=442px`라 줄바꿈이 나고 세로 배너가 첫 화면을 먹는다.
  이게 쿠팡 클릭 0의 실측 원인이었다(2026-08-19).
- **CTA는 매번 다르게.** 글을 읽어야만 뜻이 서는 문구로. 같은 문형을 두 편 연속 쓰면
  그 자체가 새 패턴으로 굳는다.
- **딥링크를 초안 본문에 직접 박지 않는다.** 초안에 `link.coupang.com`이 있으면
  `compliance.py`의 `c_disclosure`가 ERROR를 낸다. 인라인 링크도
  `assets/products/<key>-inline.html` 인클루드로 빼고 `[[PRODUCT:<key>-inline]]`을 쓴다.
- **상품 카드 HTML의 주석은 발행본에 그대로 실린다**(초안 주석만 워크플로우가 지운다).
  카드 파일에 `display:flex`·`coupangcdn` 같은 단어를 예시로도 쓰지 마라 — 검사에 걸린다.
- **외부 링크는 걸기 전에 살아 있는지 확인한다.** 세션이 막히면 `fetch-page.yml`로 러너에서 연다.
  확인 못 한 링크를 발행하고 운영자에게 눌러봐 달라고 하지 마라.

### 발행 후
`wp-set-slug-tags`(영문 슬러그) → `wp-seo-meta`(excerpt) → `wp-inspect`.
FAQ가 있으면 `wp-faq-schema`도. **Rank Math 포커스 키워드는 REST로 안 들어가 운영자가 직접 입력한다.**

## 배너는 글당 1개 (2026-08-16 개정 ★)

> 운영자: "쿠팡 파트너스 구매건이 한 개도 없어. 세 개씩 넣으니까 너무 광고성처럼 보이고 집중이 안 된다."
> "결국 이 블로그는 수익이 목적이야."

3종 카드로 다섯 편을 돌렸고 전환은 0이었다. **글당 상품 1개**로 간다.

- **상품은 글의 결론이 가리키는 것 하나.** 카드는 부록이 아니라 **본문 논지의 도착점**이다.
- **위치는 글 끝이 아니라** 그 물건이 필요하다고 납득된 문단 **바로 다음**.
- 나머지 항목은 **상품 없이 정보로만** 쓴다. 신뢰가 올라간다.
- **"셋 다 필요는 없어요" 류로 닫지 않는다.** 단점은 적되 **"이런 분께"**로 닫는다.
- 카드 구성 순서: ① 글의 결론과 잇기 ② 고를 조건 → 이 제품이 맞는 이유 ③ 쓰는 법
  ④ 솔직한 단점 ⑤ 이런 분께 ⑥ CTA 하나
- **가로 레이아웃, 이미지 크게.** 세로 배너를 나란히 놓지 않는다.

운영자에게는 **검색 링크 1개**만 요청한다. 상품군은 글의 논지로 이미 정해진다.

자세한 근거와 체크리스트는 [`docs/운영-가이드.md`](docs/운영-가이드.md) 「배너를 요청하는 방식」.

## 글마다 반드시 다르게 (★)
같은 틀을 반복하면 독자도 검색엔진도 양산형으로 본다.
새 글 쓰기 전 **스타일가이드 3.5 F 이력표**를 보고 **최근 4편과 도입·전개·마무리 조합이 겹치지 않게** 고른다.
굳어버린 상투구 목록(3.6 A)도 확인한다 — `정리하면`, `저도 ~ 기억이 나요`, `관련해서 아래 글도`, `우리 아이` 남발 등.

## 지식iN 홍보
스타일가이드 **8절**을 따른다. 핵심만:
- 답변 본문에서 특정 제품을 추천하지 않는다
- 쿠팡 링크를 답변에 직접 붙이지 않는다
- 블로그 링크를 달면 **끝부분에 제휴 관계를 공개**한다
- 답변은 **코드블록으로 전달**한다(서식이 딸려가면 배경색·글자색이 깨진다)

## 데이터 대장 (발행할 때마다 갱신)
| 파일 | 언제 |
|---|---|
| `data/content_ledger.csv` | 글 발행 시 한 줄 (제목에 쉼표 있으면 따옴표로 감쌀 것) |
| `data/image_ledger.csv` | 인포그래픽 제작 시 한 줄 |
| `templates/제휴글-스타일가이드.md` 3.5 F | 구조 조합 한 줄 |
| `docs/콘텐츠-로드맵.md` | 발행·보류 결정이 바뀔 때 |
